<div class="min-h-screen bg-rose-50 py-10 px-4 sm:px-8">
  <div class="max-w-6xl mx-auto">
    <div class="bg-white rounded-2xl shadow-md p-6 mb-8 border border-rose-200">
      <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4">
        <div>
          <h1 class="text-4xl font-extrabold text-rose-800"><%= @teacher.first_name %> <%= @teacher.last_name %></h1>
          <p class="text-lg text-rose-700 mt-1">Email: <%= @teacher.email_address %></p>
        </div>
      </div>
    </div>

    <h3 class="text-2xl font-semibold text-rose-700 mb-4">Classrooms</h3>
    <%= render "classrooms/classrooms", classrooms: @teacher.classrooms %>
  </div>
</div>